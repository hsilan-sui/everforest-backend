# .editorconfig
root = true

[*]
end_of_line = lf              # 換行統一 LF
charset = utf-8              # 編碼統一 utf-8
indent_style = space         # 使用空格
indent_size = 2              # 空格長度為 2
insert_final_newline = true  # 結尾加空行
trim_trailing_whitespace = true # 清尾端空白